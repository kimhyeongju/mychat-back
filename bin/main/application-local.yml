spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/mychat_local
    username: mychat
    password: 753951
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
