spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: test-only-secret-key-please-change-this-in-real-env-32bytes+
  access-token-expiration-ms: 1800000
  refresh-token-expiration-ms: 1209600000

sms:
  aligo:
    api-key: test
    user-id: test
    sender: 01000000000
    mock: true
