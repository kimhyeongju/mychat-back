rootProject.name = 'khj-playground-back'

// 의존 방향: app -> (chat, board, invest) -> auth -> common
// 서비스 모듈끼리는 서로 include/참조하지 않는다.
include 'common'
include 'auth'
include 'chat'

// 아직 구현 전이라면 주석 처리해두고, 착수 시점에 해제한다.
// include 'board'
// include 'invest'

include 'app'